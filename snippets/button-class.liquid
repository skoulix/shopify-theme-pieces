{%- comment -%}
  Button Class Helper
  Calculates and outputs the appropriate button class based on style and color settings.

  Usage:
  <a class="btn {% render 'button-class', style: section.settings.button_style, color: section.settings.button_color %}">

  Parameters:
  - style: 'solid' or 'outline'
  - color: 'primary' or 'secondary'

  Output Classes:
  - solid + primary = btn--primary
  - solid + secondary = btn--solid-secondary
  - outline + primary = btn--outline
  - outline + secondary = btn--secondary
{%- endcomment -%}
{%- liquid
  if style == 'outline'
    if color == 'secondary'
      echo 'btn--secondary'
    else
      echo 'btn--outline'
    endif
  else
    if color == 'secondary'
      echo 'btn--solid-secondary'
    else
      echo 'btn--primary'
    endif
  endif
-%}
