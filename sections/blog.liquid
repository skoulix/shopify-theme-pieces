{%- liquid
  capture container_class
    render 'container-class', full_width: section.settings.full_width
  endcapture
-%}

<style>
  /* Title line stagger (managed by TweenManager) */
  .blog-wrapper .page-title .tween-split-line:nth-child(2) { margin-left: clamp(1.5rem, 8vw, 6rem); }
  .blog-wrapper .page-title .tween-split-line:nth-child(3) { margin-left: clamp(3rem, 16vw, 12rem); }
  .blog-item-author::before { content: ''; display: inline-block; width: 4px; height: 4px; border-radius: 50%; background: var(--color-text-secondary); margin-right: 0.5rem; vertical-align: middle; }
</style>

<section class="blog-wrapper page-padding" data-blog-wrapper>
  <div class="section-header-spacing" data-tween-group="blog-header-{{ section.id }}">
    <div class="{{ container_class }}">
      <h1 class="page-title" data-tween data-tween-type="split-text">
        {{ blog.title }}
      </h1>

      {% if section.settings.show_article_count %}
        <p class="page-header-subtitle">
          <span class="page-header-subtitle-line" data-subtitle-line></span>
          <span class="overflow-hidden"><span class="inline-block" data-subtitle-text>{{ blog.articles_count }} {{ blog.articles_count | pluralize: 'Article', 'Articles' }}</span></span>
        </p>
      {% endif %}
    </div>
  </div>

  {% paginate blog.articles by section.settings.articles_per_page %}
    <div class="{{ container_class }}">
      <div class="grid gap-12 md:grid-cols-2 md:gap-8 lg:grid-cols-3 lg:gap-10" data-blog-grid>
        {% for article in blog.articles %}
          {%- assign is_featured = false -%}
          {%- if forloop.first and section.settings.feature_first -%}
            {%- assign is_featured = true -%}
          {%- endif -%}
          <article
            class="blog-item flex flex-col bg-(--color-background-secondary) rounded-(--card-radius) overflow-hidden border-(--card-border-width) border-solid border-(--color-border) shadow-(--card-shadow){% if is_featured %} blog-item--featured col-span-full lg:grid lg:grid-cols-2 lg:gap-16 lg:items-stretch{% endif %}"
            data-blog-item="{{ forloop.index0 }}"
            data-article-url="{{ article.url }}"
          >
            <a href="{{ article.url }}" class="blog-item-image block aspect-[16/10] overflow-hidden{% if is_featured %} aspect-[16/9] lg:aspect-auto lg:h-full{% endif %}" aria-label="{{ article.title }}" data-tween data-tween-type="clip-right">
              {% if article.image %}
                {{ article.image | image_url: width: 1600 | image_tag:
                  class: 'w-full h-full object-cover transition-transform duration-700 ease-out hover:scale-105',
                  loading: 'lazy',
                  decoding: 'async',
                  sizes: '(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 33vw',
                  widths: '400, 600, 800, 1000, 1200, 1600',
                  alt: article.image.alt | default: article.title
                }}
              {% else %}
                <div class="w-full h-full flex items-center justify-center bg-(--color-background-secondary)">
                  <i class="ph ph-article text-5xl text-(--color-text-secondary) opacity-30"></i>
                </div>
              {% endif %}
            </a>

            <div class="p-6 flex flex-col flex-1{% if is_featured %} lg:py-8 lg:pr-8 lg:pl-0 lg:justify-center{% endif %}" data-tween-group="blog-card-{{ forloop.index0 }}">
              {% if article.tags.size > 0 %}
                <p class="blog-item-category blog-category" data-tween data-tween-type="fade-up">
                  {{ article.tags.first }}
                </p>
              {% endif %}

              <h2 class="blog-card-title{% if is_featured %} blog-card-title--featured{% endif %}">
                <a href="{{ article.url }}" class="block hover:text-(--color-primary) transition-colors">
                  <span data-tween data-tween-type="split-text">{{ article.title }}</span>
                </a>
              </h2>

              <div class="blog-item-meta mt-2 flex items-center gap-4 text-xs text-(--color-text-secondary)" data-tween data-tween-type="fade-up">
                <time datetime="{{ article.published_at | date: '%Y-%m-%d' }}">
                  {{ article.published_at | date: '%b %d, %Y' }}
                </time>
                {% if section.settings.show_author %}
                  <span class="blog-item-author flex items-center gap-1.5">
                    <span class="w-1 h-1 rounded-full bg-current opacity-40"></span>
                    {{ article.author }}
                  </span>
                {% endif %}
              </div>

              {% if section.settings.show_excerpt %}
                <p class="blog-item-excerpt mt-3 text-sm leading-relaxed text-(--color-text-secondary) line-clamp-3{% if is_featured %} lg:text-[0.9375rem] lg:line-clamp-6{% endif %}" data-tween data-tween-type="fade-up">
                  {{ article.excerpt_or_content | strip_html | truncate: 160 }}
                </p>
              {% endif %}

              <a href="{{ article.url }}" class="link-cta mt-auto pt-4 uppercase" data-tween data-tween-type="fade-up">
                <span class="link-cta-text"><span data-hover="{{ 'blog.read_article' | t }}">{{ 'blog.read_article' | t }}</span></span>
                <span class="sr-only">: {{ article.title }}</span>
                <i class="ph ph-arrow-right" aria-hidden="true"></i>
              </a>
            </div>
          </article>
        {% endfor %}
      </div>
    </div>

    {% if paginate.pages > 1 %}
      <nav class="{{ container_class }} flex items-center justify-center gap-2 mt-12">
        {% if paginate.previous %}
          <a href="{{ paginate.previous.url }}" class="w-10 h-10 flex items-center justify-center rounded-full border border-(--color-border) text-(--color-text) hover:bg-(--color-background-secondary) transition-colors" aria-label="{{ 'accessibility.previous_page' | t }}">
            <i class="ph ph-arrow-left"></i>
          </a>
        {% endif %}

        {% for part in paginate.parts %}
          {% if part.is_link %}
            <a href="{{ part.url }}" class="w-10 h-10 flex items-center justify-center text-sm text-(--color-text-secondary) hover:text-(--color-text) transition-colors">{{ part.title }}</a>
          {% else %}
            {% if part.title == paginate.current_page %}
              <span class="w-10 h-10 flex items-center justify-center text-sm font-semibold text-(--color-text) bg-(--color-background-secondary) rounded-full">{{ part.title }}</span>
            {% else %}
              <span class="w-10 h-10 flex items-center justify-center text-sm text-(--color-text-secondary)">{{ part.title }}</span>
            {% endif %}
          {% endif %}
        {% endfor %}

        {% if paginate.next %}
          <a href="{{ paginate.next.url }}" class="w-10 h-10 flex items-center justify-center rounded-full border border-(--color-border) text-(--color-text) hover:bg-(--color-background-secondary) transition-colors" aria-label="{{ 'accessibility.next_page' | t }}">
            <i class="ph ph-arrow-right"></i>
          </a>
        {% endif %}
      </nav>
    {% endif %}
  {% endpaginate %}
</section>

<script>
(function() {
  if (document.documentElement.classList.contains('animations-disabled')) return;

  const wrapper = document.querySelector('[data-blog-wrapper]');
  if (!wrapper) return;

  const initAnimation = () => {
    if (typeof gsap === 'undefined') {
      requestAnimationFrame(initAnimation);
      return;
    }

    const tl = gsap.timeline();

    // Subtitle line draw animation
    const subtitleLine = wrapper.querySelector('[data-subtitle-line]');
    if (subtitleLine) {
      gsap.set(subtitleLine, { scaleX: 0, transformOrigin: 'left center' });
      tl.to(subtitleLine, {
        scaleX: 1,
        duration: 0.8,
        ease: 'power3.out'
      }, 0.4);
    }

    // Subtitle text reveal
    const subtitleText = wrapper.querySelector('[data-subtitle-text]');
    if (subtitleText) {
      gsap.set(subtitleText, { yPercent: 120 });
      tl.to(subtitleText, {
        yPercent: 0,
        duration: 0.8,
        ease: 'power4.out'
      }, 0.6);
    }
  };

  initAnimation();
})();
</script>

{% schema %}
{
  "name": "Blog",
  "settings": [
    {
      "type": "checkbox",
      "id": "full_width",
      "default": false,
      "label": "Full width"
    },
    {
      "type": "range",
      "id": "articles_per_page",
      "min": 3,
      "max": 24,
      "step": 3,
      "default": 9,
      "label": "Articles per page"
    },
    {
      "type": "checkbox",
      "id": "feature_first",
      "default": true,
      "label": "Feature first article"
    },
    {
      "type": "checkbox",
      "id": "show_excerpt",
      "default": true,
      "label": "Show excerpt"
    },
    {
      "type": "checkbox",
      "id": "show_author",
      "default": true,
      "label": "Show author"
    },
    {
      "type": "checkbox",
      "id": "show_article_count",
      "default": true,
      "label": "Show article count"
    }
  ],
  "disabled_on": {
    "groups": ["header", "footer"]
  }
  }
{% endschema %}
