{%- comment -%}
  Slideshow Section
  Full-width image/video slideshow with text overlays
  Features: autoplay, navigation dots/arrows, parallax effect, GSAP transitions
{%- endcomment -%}

{%- liquid
  assign section_height = section.settings.section_height
  assign autoplay = section.settings.autoplay
  assign autoplay_speed = section.settings.autoplay_speed
  assign show_arrows = section.settings.show_arrows
  assign show_dots = section.settings.show_dots
  assign parallax = section.settings.parallax
  assign overlay_color = section.settings.overlay_color
  assign overlay_opacity = section.settings.overlay_opacity
  assign overlay_alpha = overlay_opacity | divided_by: 100.0
  assign transition_effect = section.settings.transition_effect
  assign progress_type = section.settings.progress_type
  assign pause_on_hover = section.settings.pause_on_hover
-%}

<style>
  #slideshow-{{ section.id }} {
    --slideshow-height: {{ section_height }}dvh;
  }
  @supports not (height: 1dvh) {
    #slideshow-{{ section.id }} { --slideshow-height: {{ section_height }}vh; }
  }

  /* Base slide states */
  #slideshow-{{ section.id }} .slideshow__slide {
    opacity: 0;
    visibility: hidden;
    z-index: 0;
    backface-visibility: hidden;
    -webkit-backface-visibility: hidden;
  }
  #slideshow-{{ section.id }} .slideshow__slide.is-active {
    opacity: 1;
    visibility: visible;
    z-index: 1;
  }
  #slideshow-{{ section.id }} .slideshow__media,
  #slideshow-{{ section.id }} .slideshow__media-wrapper {
    backface-visibility: hidden;
    -webkit-backface-visibility: hidden;
    will-change: transform;
  }

  /* Navigation dots with circular timer */
  #slideshow-{{ section.id }} .slideshow__dot.is-active {
    background: var(--color-text);
  }
  #slideshow-{{ section.id }} .slideshow__dots {
    color: var(--color-text);
  }
  #slideshow-{{ section.id }} .slideshow__dot-timer {
    transition: opacity 0.3s ease;
  }
  #slideshow-{{ section.id }} .slideshow__dot.is-active + .slideshow__dot-timer {
    opacity: 1 !important;
  }
  #slideshow-{{ section.id }} .slideshow__dot-timer .timer-progress {
    transform: rotate(-90deg);
    transform-origin: 14px 14px;
    stroke-dasharray: 75.4;
    stroke-dashoffset: 75.4;
  }
  #slideshow-{{ section.id }} .slideshow__dot-timer .timer-progress.is-animating {
    animation: dot-timer-{{ section.id }} {{ autoplay_speed }}s linear forwards;
  }
  @keyframes dot-timer-{{ section.id }} {
    from { stroke-dashoffset: 75.4; }
    to { stroke-dashoffset: 0; }
  }
  #slideshow-{{ section.id }} .slideshow__progress.is-animating {
    animation: slideshow-progress-{{ section.id }} {{ autoplay_speed }}s linear forwards;
  }
  @keyframes slideshow-progress-{{ section.id }} {
    from { width: 0; }
    to { width: 100%; }
  }
  @media (min-width: 768px) {
    #slideshow-{{ section.id }} .slideshow__arrow { opacity: 0; }
    #slideshow-{{ section.id }} .slideshow:hover .slideshow__arrow { opacity: 1; }
  }

  /* Navigation styling - inherits from section color scheme */
  #slideshow-{{ section.id }} .slideshow__arrow {
    background: var(--color-background);
    color: var(--color-text);
  }
  #slideshow-{{ section.id }} .slideshow__arrow:hover {
    background: var(--color-background-secondary);
  }
  #slideshow-{{ section.id }} .slideshow__dot {
    background: color-mix(in srgb, var(--color-text) 40%, transparent);
  }
  #slideshow-{{ section.id }} .slideshow__dot:hover {
    background: color-mix(in srgb, var(--color-text) 80%, transparent);
  }
  #slideshow-{{ section.id }} .slideshow__dot.is-active {
    background: var(--color-text);
  }
  #slideshow-{{ section.id }} .slideshow__dots {
    color: var(--color-text);
  }
  #slideshow-{{ section.id }} .slideshow__progress {
    background: color-mix(in srgb, var(--color-text) 60%, transparent);
  }

  /* Button styling - secondary buttons use text/background for visibility */
  #slideshow-{{ section.id }} .btn--secondary {
    color: var(--color-text);
    border-color: var(--color-text);
  }
  #slideshow-{{ section.id }} .btn--secondary::before {
    background: var(--color-text);
  }
  #slideshow-{{ section.id }} .btn--secondary:hover,
  #slideshow-{{ section.id }} .btn--secondary:hover span,
  #slideshow-{{ section.id }} .btn--secondary:hover i {
    color: var(--color-background);
  }
  #slideshow-{{ section.id }} .btn--solid-secondary {
    background: var(--color-text);
    border-color: var(--color-text);
    color: var(--color-background);
  }
  #slideshow-{{ section.id }} .btn--solid-secondary::before {
    background: var(--color-background);
  }
  #slideshow-{{ section.id }} .btn--solid-secondary:hover,
  #slideshow-{{ section.id }} .btn--solid-secondary:hover span,
  #slideshow-{{ section.id }} .btn--solid-secondary:hover i {
    color: var(--color-text);
  }

  {% if parallax %}
  #slideshow-{{ section.id }} .slideshow__media-wrapper { height: 120%; top: -10%; }
  {% endif %}

  /* Initial loading state - hide content until JS animates it */
  #slideshow-{{ section.id }} .slideshow__label,
  #slideshow-{{ section.id }} .slideshow__title,
  #slideshow-{{ section.id }} .slideshow__description,
  #slideshow-{{ section.id }} .slideshow__cta { opacity: 0; }
</style>

<div id="slideshow-{{ section.id }}" class="slideshow-section {{ section.blocks.first.settings.color_scheme | prepend: 'color-' }}" data-slideshow data-transition="{{ transition_effect }}">
  {%- comment -%} Screen reader announcements for slide changes {%- endcomment -%}
  <div class="sr-only" aria-live="polite" aria-atomic="true" data-slideshow-status></div>
  <div class="slideshow relative overflow-hidden h-(--slideshow-height) min-h-[400px] bg-(--color-background)" aria-label="{{ 'accessibility.slideshow' | t }}" role="region">
    {%- comment -%} Slides {%- endcomment -%}
    {% for block in section.blocks %}
      {%- liquid
        assign text_position = block.settings.text_position
        assign text_alignment = block.settings.text_alignment
        assign slide_color_scheme = 'color-' | append: block.settings.color_scheme

        case text_position
          when 'top_left'
            assign content_justify = 'justify-start'
            assign content_items = 'items-start'
          when 'top_center'
            assign content_justify = 'justify-start'
            assign content_items = 'items-center'
          when 'top_right'
            assign content_justify = 'justify-start'
            assign content_items = 'items-end'
          when 'middle_left'
            assign content_justify = 'justify-center'
            assign content_items = 'items-start'
          when 'middle_center'
            assign content_justify = 'justify-center'
            assign content_items = 'items-center'
          when 'middle_right'
            assign content_justify = 'justify-center'
            assign content_items = 'items-end'
          when 'bottom_left'
            assign content_justify = 'justify-end'
            assign content_items = 'items-start'
          when 'bottom_center'
            assign content_justify = 'justify-end'
            assign content_items = 'items-center'
          when 'bottom_right'
            assign content_justify = 'justify-end'
            assign content_items = 'items-end'
        endcase
      -%}

      <div
        class="slideshow__slide {{ slide_color_scheme }} absolute inset-0 bg-(--color-background){% if forloop.first %} is-active{% endif %}"
        data-slide-index="{{ forloop.index0 }}"
        data-color-scheme="{{ slide_color_scheme }}"
        {{ block.shopify_attributes }}
      >
        {%- comment -%} Media wrapper with overflow hidden to contain zoom effects {%- endcomment -%}
        <div class="slideshow__media-wrapper absolute inset-0 overflow-hidden">
          {% if block.settings.video != blank %}
            {{ block.settings.video | video_tag:
              class: 'slideshow__media absolute inset-0 w-full h-full object-cover',
              autoplay: true,
              muted: true,
              loop: true,
              playsinline: true,
              poster: block.settings.image
            }}
          {% elsif block.settings.image != blank %}
            {%- assign img_aspect = block.settings.image.aspect_ratio | default: 1.78 -%}
            {%- assign img_height = 1920 | divided_by: img_aspect | round -%}
            <img
              src="{{ block.settings.image | image_url: width: 1920 }}"
              srcset="{{ block.settings.image | image_url: width: 768 }} 768w,
                      {{ block.settings.image | image_url: width: 1200 }} 1200w,
                      {{ block.settings.image | image_url: width: 1920 }} 1920w"
              sizes="100vw"
              alt="{{ block.settings.image.alt | escape }}"
              class="slideshow__media absolute inset-0 w-full h-full object-cover"
              width="1920"
              height="{{ img_height }}"
              loading="{% if forloop.first %}eager{% else %}lazy{% endif %}"
              fetchpriority="{% if forloop.first %}high{% else %}low{% endif %}"
              decoding="async"
              {% if parallax %}data-parallax-media{% endif %}
            >
          {% else %}
            <div class="slideshow__media absolute inset-0 w-full h-full">
              {{ 'lifestyle-1' | placeholder_svg_tag: 'w-full h-full object-cover' }}
            </div>
          {% endif %}
          {%- comment -%} Overlay {%- endcomment -%}
          <div class="slideshow__overlay absolute inset-0 pointer-events-none" style="background-color: {{ overlay_color | color_modify: 'alpha', overlay_alpha }};"></div>
        </div>

        {%- comment -%} Content {%- endcomment -%}
        {%- liquid
          if block.settings.full_width_content
            assign content_container = 'container-fluid'
          else
            assign content_container = 'page-container'
          endif
        -%}
        <div class="slideshow__content relative z-1 h-full flex flex-col {{ content_container }} {{ content_justify }} {{ content_items }}">
          <div class="text-{{ text_alignment }}">
            {% if block.settings.label != blank %}
              <p class="slideshow__label text-sm{% if settings.headings_uppercase %} uppercase{% endif %} tracking-wider mb-4 opacity-80">
                {{ block.settings.label }}
              </p>
            {% endif %}

            {% if block.settings.title != blank %}
              <h2 class="slideshow__title hero-title mb-6 text-balance">
                {{ block.settings.title }}
              </h2>
            {% endif %}

            {% if block.settings.text != blank %}
              <p class="slideshow__description text-lg opacity-90 mb-8 text-balance">
                {{ block.settings.text }}
              </p>
            {% endif %}

            {% if block.settings.button_text != blank and block.settings.button_link != blank %}
              <div class="slideshow__cta">
                {% render 'button',
                  text: block.settings.button_text,
                  url: block.settings.button_link,
                  style: block.settings.button_style,
                  color: block.settings.button_color,
                  size: block.settings.button_size
                %}
              </div>
            {% endif %}
          </div>
        </div>
      </div>
    {% endfor %}

    {%- comment -%} Navigation - Combined on mobile, separate on desktop {%- endcomment -%}
    {% if section.blocks.size > 1 %}
      {%- comment -%} Mobile Navigation: arrows + dots in one row at bottom {%- endcomment -%}
      <div class="slideshow__mobile-nav md:hidden absolute bottom-8 left-1/2 -translate-x-1/2 z-10 flex items-center gap-4">
        {% if show_arrows %}
          <button
            type="button"
            class="slideshow__arrow slideshow__nav w-12 h-12 flex items-center justify-center backdrop-blur-md rounded-full cursor-pointer transition-all duration-300"
            data-slideshow-prev
            aria-label="{{ 'accessibility.previous_slide' | t }}"
          >
            <i class="ph ph-caret-left text-xl"></i>
          </button>
        {% endif %}

        {% if show_dots %}
          <div class="slideshow__dots flex items-center gap-5">
            {% for block in section.blocks %}
              <div class="slideshow__dot-wrapper relative">
                <button
                  type="button"
                  class="slideshow__dot block rounded-full border-0 cursor-pointer transition-all duration-300 w-2.5 h-2.5{% if forloop.first %} is-active{% endif %}"
                  data-slideshow-dot="{{ forloop.index0 }}"
                  aria-label="{{ 'accessibility.go_to_slide' | t: index: forloop.index }}"
                ></button>
                {% if autoplay and progress_type == 'dots' %}
                <div class="slideshow__dot-timer" style="position:absolute;top:-9px;left:-9px;width:28px;height:28px;pointer-events:none;opacity:0;">
                  <svg width="28" height="28" viewBox="0 0 28 28" style="display:block;">
                    <circle cx="14" cy="14" r="12" fill="none" stroke="currentColor" stroke-opacity="0.3" stroke-width="2"/>
                    <circle class="timer-progress" cx="14" cy="14" r="12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" data-dot-progress/>
                  </svg>
                </div>
                {% endif %}
              </div>
            {% endfor %}
          </div>
        {% endif %}

        {% if show_arrows %}
          <button
            type="button"
            class="slideshow__arrow slideshow__nav w-12 h-12 flex items-center justify-center backdrop-blur-md rounded-full cursor-pointer transition-all duration-300"
            data-slideshow-next
            aria-label="{{ 'accessibility.next_slide' | t }}"
          >
            <i class="ph ph-caret-right text-xl"></i>
          </button>
        {% endif %}
      </div>

      {%- comment -%} Desktop Navigation: arrows on sides {%- endcomment -%}
      {% if show_arrows %}
        <button
          type="button"
          class="slideshow__arrow slideshow__nav hidden md:flex absolute top-1/2 -translate-y-1/2 left-8 z-10 w-12 h-12 items-center justify-center backdrop-blur-md rounded-full cursor-pointer transition-all duration-300"
          data-slideshow-prev
          aria-label="{{ 'accessibility.previous_slide' | t }}"
        >
          <i class="ph ph-caret-left text-xl"></i>
        </button>
        <button
          type="button"
          class="slideshow__arrow slideshow__nav hidden md:flex absolute top-1/2 -translate-y-1/2 right-8 z-10 w-12 h-12 items-center justify-center backdrop-blur-md rounded-full cursor-pointer transition-all duration-300"
          data-slideshow-next
          aria-label="{{ 'accessibility.next_slide' | t }}"
        >
          <i class="ph ph-caret-right text-xl"></i>
        </button>
      {% endif %}

      {%- comment -%} Desktop Navigation: dots centered at bottom {%- endcomment -%}
      {% if show_dots %}
        <div class="slideshow__dots slideshow__nav hidden md:flex absolute bottom-8 left-1/2 -translate-x-1/2 z-10 items-center gap-5">
          {% for block in section.blocks %}
            <div class="slideshow__dot-wrapper relative">
              <button
                type="button"
                class="slideshow__dot block rounded-full border-0 cursor-pointer transition-all duration-300 w-2.5 h-2.5{% if forloop.first %} is-active{% endif %}"
                data-slideshow-dot="{{ forloop.index0 }}"
                aria-label="{{ 'accessibility.go_to_slide' | t: index: forloop.index }}"
              ></button>
              {% if autoplay and progress_type == 'dots' %}
              <div class="slideshow__dot-timer" style="position:absolute;top:-9px;left:-9px;width:28px;height:28px;pointer-events:none;opacity:0;">
                <svg width="28" height="28" viewBox="0 0 28 28" style="display:block;">
                  <circle cx="14" cy="14" r="12" fill="none" stroke="currentColor" stroke-opacity="0.3" stroke-width="2"/>
                  <circle class="timer-progress" cx="14" cy="14" r="12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" data-dot-progress/>
                </svg>
              </div>
              {% endif %}
            </div>
          {% endfor %}
        </div>
      {% endif %}
    {% endif %}

    {%- comment -%} Progress Bar {%- endcomment -%}
    {% if autoplay and section.blocks.size > 1 and progress_type == 'bar' %}
      <div class="slideshow__progress slideshow__nav absolute bottom-0 left-0 h-[3px] z-10 w-0" data-slideshow-progress></div>
    {% endif %}
  </div>
</div>

<script>
  (function() {
    const container = document.querySelector('#slideshow-{{ section.id }}');
    if (!container || container.dataset.slideshowInitialized) return;
    container.dataset.slideshowInitialized = 'true';

    // Wait for GSAP and SplitText
    function initSlideshow() {
      if (typeof gsap === 'undefined') {
        if (window.pieces && window.pieces.gsap) {
          window.gsap = window.pieces.gsap;
        } else {
          requestAnimationFrame(initSlideshow);
          return;
        }
      }

      // Get SplitText from pieces or window
      let SplitText = window.SplitText;
      if (!SplitText && window.pieces && window.pieces.SplitText) {
        SplitText = window.pieces.SplitText;
      }

      if (!SplitText) {
        requestAnimationFrame(initSlideshow);
        return;
      }

      const slides = container.querySelectorAll('[data-slide-index]');
      const dots = container.querySelectorAll('[data-slideshow-dot]');
      const prevBtns = container.querySelectorAll('[data-slideshow-prev]');
      const nextBtns = container.querySelectorAll('[data-slideshow-next]');
      const progress = container.querySelector('[data-slideshow-progress]');
      const parallaxMedia = container.querySelectorAll('[data-parallax-media]');
      const transition = container.dataset.transition || 'fade';
      const totalSlides = slides.length;

      // Split titles for line-by-line animation
      const titleSplits = new Map();
      slides.forEach(slide => {
        const title = slide.querySelector('.slideshow__title');
        if (title) {
          const split = new SplitText(title, { type: 'lines', linesClass: 'slideshow__title-line' });
          // Wrap each line for overflow hidden reveal
          // Add padding to prevent descender letters (y, g, p, q) from being clipped
          split.lines.forEach(line => {
            const wrapper = document.createElement('div');
            wrapper.style.overflow = 'hidden';
            wrapper.style.display = 'block';
            wrapper.style.paddingBottom = '0.25em';
            wrapper.style.marginBottom = '-0.25em';
            line.parentNode.insertBefore(wrapper, line);
            wrapper.appendChild(line);
          });
          // Set lines off-screen BEFORE making title visible
          gsap.set(split.lines, { yPercent: 120 });
          // Now make title visible (lines are already positioned off-screen)
          title.style.opacity = 1;
          titleSplits.set(slide, split);
        }
      });

      let currentSlide = 0;
      let autoplayInterval = null;
      let isPaused = false;
      let isAnimating = false;
      const autoplayEnabled = {{ autoplay }};
      const autoplaySpeed = {{ autoplay_speed }} * 1000;

      // Transition configurations
      // Helper to get media element
      const getMediaElement = (slide) => slide.querySelector('.slideshow__media');

      const transitions = {
        fade: {
          duration: 0.8,
          enter: (slide) => gsap.fromTo(slide, { opacity: 0 }, { opacity: 1, duration: 0.8, ease: 'power2.inOut' }),
          exit: (slide) => gsap.to(slide, { opacity: 0, duration: 0.8, ease: 'power2.inOut' })
        },
        slide: {
          duration: 1.2,
          enter: (slide, dir) => {
            const media = getMediaElement(slide);
            // Slide enters from 100%
            gsap.set(slide, { xPercent: dir === 'next' ? 100 : -100, opacity: 1, visibility: 'visible' });
            // Media scaled up and offset in opposite direction - creates parallax as it settles
            if (media) {
              gsap.set(media, { scale: 1.2, xPercent: dir === 'next' ? -20 : 20, transformOrigin: 'center center' });
              gsap.to(media, { scale: 1, xPercent: 0, duration: 1.4, ease: 'power2.out' });
            }
            return gsap.to(slide, { xPercent: 0, duration: 1.2, ease: 'power3.inOut' });
          },
          exit: (slide, dir) => {
            const media = getMediaElement(slide);
            // Media moves slower than slide, creating depth
            if (media) gsap.to(media, { xPercent: dir === 'next' ? 20 : -20, scale: 1.1, transformOrigin: 'center center', duration: 1.2, ease: 'power2.inOut' });
            return gsap.to(slide, { xPercent: dir === 'next' ? -100 : 100, duration: 1.2, ease: 'power3.inOut' });
          }
        },
        'zoom-in': {
          duration: 1.4,
          enter: (slide) => {
            const media = getMediaElement(slide);
            gsap.set(slide, { opacity: 0, visibility: 'visible' });
            if (media) gsap.set(media, { scale: 1, transformOrigin: 'center center' });
            // Fade in slide while zooming media
            gsap.to(slide, { opacity: 1, duration: 0.8, ease: 'power2.out' });
            return media ? gsap.to(media, { scale: 1.15, duration: 1.4, ease: 'power2.out' }) : null;
          },
          exit: (slide) => {
            const media = getMediaElement(slide);
            // Fade out while continuing zoom slightly
            if (media) gsap.to(media, { scale: 1.2, duration: 0.6, ease: 'power2.in' });
            return gsap.to(slide, { opacity: 0, duration: 0.6, ease: 'power2.in' });
          }
        },
        'zoom-out': {
          duration: 1.4,
          enter: (slide) => {
            const media = getMediaElement(slide);
            gsap.set(slide, { opacity: 0, visibility: 'visible' });
            if (media) gsap.set(media, { scale: 1.3, transformOrigin: 'center center' });
            // Fade in slide while zooming out media
            gsap.to(slide, { opacity: 1, duration: 0.8, ease: 'power2.out' });
            return media ? gsap.to(media, { scale: 1, duration: 1.4, ease: 'power2.out' }) : null;
          },
          exit: (slide) => {
            const media = getMediaElement(slide);
            // Fade out while continuing to zoom out slightly
            if (media) gsap.to(media, { scale: 0.95, duration: 0.6, ease: 'power2.in' });
            return gsap.to(slide, { opacity: 0, duration: 0.6, ease: 'power2.in' });
          }
        },
        swipe: {
          duration: 1.2,
          enter: (slide, dir) => {
            const media = getMediaElement(slide);
            // Next: reveal from right (clip left side), Prev: reveal from left (clip right side)
            gsap.set(slide, { clipPath: dir === 'next' ? 'inset(0 0 0 100%)' : 'inset(0 100% 0 0)', opacity: 1, visibility: 'visible' });
            // Media parallax - offset in opposite direction and scale up
            if (media) {
              gsap.set(media, { scale: 1.15, xPercent: dir === 'next' ? 15 : -15, transformOrigin: 'center center' });
              gsap.to(media, { scale: 1, xPercent: 0, duration: 1.4, ease: 'power2.out' });
            }
            return gsap.to(slide, { clipPath: 'inset(0 0% 0 0%)', duration: 1.2, ease: 'power4.inOut' });
          },
          exit: (slide, dir) => {
            const media = getMediaElement(slide);
            if (media) gsap.to(media, { xPercent: dir === 'next' ? -15 : 15, scale: 1.1, transformOrigin: 'center center', duration: 1.2, ease: 'power2.inOut' });
            return gsap.to(slide, {
              clipPath: dir === 'next' ? 'inset(0 100% 0 0)' : 'inset(0 0 0 100%)',
              duration: 1.2,
              ease: 'power4.inOut'
            });
          }
        }
      };
      // Alias for backwards compatibility
      transitions.reveal = transitions.swipe;

      function animateContent(slide, show = true) {
        const label = slide.querySelector('.slideshow__label');
        const description = slide.querySelector('.slideshow__description');
        const cta = slide.querySelector('.slideshow__cta');
        const split = titleSplits.get(slide);

        if (show) {
          const tl = gsap.timeline();
          let delay = 0.3;

          // Label fade up
          if (label) {
            tl.fromTo(label, { y: 30, opacity: 0 }, { y: 0, opacity: 1, duration: 0.8, ease: 'power3.out' }, delay);
            delay += 0.1;
          }

          // Title line-by-line reveal
          if (split && split.lines.length) {
            gsap.set(split.lines, { yPercent: 120 });
            tl.to(split.lines, { yPercent: 0, duration: 1, ease: 'power4.out', stagger: 0.1 }, delay);
            delay += 0.2;
          }

          // Description fade up
          if (description) {
            tl.fromTo(description, { y: 30, opacity: 0 }, { y: 0, opacity: 1, duration: 0.8, ease: 'power3.out' }, delay + 0.1);
          }

          // CTA fade up
          if (cta) {
            tl.fromTo(cta, { y: 20, opacity: 0 }, { y: 0, opacity: 1, duration: 0.6, ease: 'power3.out' }, delay + 0.25);
          }
        } else {
          // Animate out - simpler exit
          const elements = [label, description, cta].filter(Boolean);
          if (split && split.lines.length) {
            gsap.to(split.lines, { yPercent: -120, duration: 0.4, ease: 'power2.in' });
          }
          gsap.to(elements, { y: -20, opacity: 0, duration: 0.4, ease: 'power2.in' });
        }
      }

      // Update section color scheme to match current slide
      function updateColorScheme(slide) {
        const newScheme = slide.dataset.colorScheme;
        if (!newScheme) return;

        // Remove old color scheme classes and add new one
        const section = container.closest('.slideshow-section');
        if (section) {
          // Remove any existing color-* class
          section.className = section.className.replace(/color-\S+/g, '').trim();
          section.classList.add(newScheme);
        }
      }

      function showSlide(index, direction = 'next') {
        if (isAnimating || index === currentSlide) return;
        isAnimating = true;

        const prevIndex = currentSlide;
        const prevSlide = slides[prevIndex];
        const nextSlide = slides[index];
        const trans = transitions[transition] || transitions.fade;

        // Update section color scheme to match incoming slide
        updateColorScheme(nextSlide);

        // Animate out content first
        animateContent(prevSlide, false);

        // Setup z-index
        gsap.set(nextSlide, { zIndex: 1, visibility: 'visible' });
        gsap.set(prevSlide, { zIndex: 0 });

        // Run transitions
        const enterAnim = trans.enter(nextSlide, direction);
        trans.exit(prevSlide, direction);

        // Animate in content
        animateContent(nextSlide, true);

        // Update classes after animation
        setTimeout(() => {
          slides.forEach((slide, i) => {
            slide.classList.toggle('is-active', i === index);
            if (i !== index) {
              gsap.set(slide, { clearProps: 'all' });
              const media = slide.querySelector('.slideshow__media');
              if (media) gsap.set(media, { clearProps: 'transform' });
              slide.style.opacity = '0';
              slide.style.visibility = 'hidden';
              slide.style.zIndex = '0';
            }
          });
          gsap.set(nextSlide, { opacity: 1, visibility: 'visible', zIndex: 1 });
          isAnimating = false;
        }, trans.duration * 1000);

        // Update dots and their timers (handles both mobile and desktop nav)
        dots.forEach(dot => {
          const dotIndex = parseInt(dot.dataset.slideshowDot, 10);
          dot.classList.toggle('is-active', dotIndex === index);
          // Find timer via parent wrapper
          const wrapper = dot.closest('.slideshow__dot-wrapper');
          const dotProgress = wrapper?.querySelector('[data-dot-progress]');
          if (dotProgress) {
            dotProgress.classList.remove('is-animating');
            if (dotIndex === index && autoplayEnabled) {
              void dotProgress.getBoundingClientRect(); // Force reflow
              dotProgress.classList.add('is-animating');
            }
          }
        });
        currentSlide = index;

        // Announce slide change to screen readers
        const statusEl = section.querySelector('[data-slideshow-status]');
        if (statusEl) {
          statusEl.textContent = '{{ "accessibility.slide_x_of_y" | t }}'.replace('{{ current }}', index + 1).replace('{{ total }}', totalSlides);
        }

        // Reset progress bar
        if (progress && autoplayEnabled) {
          progress.classList.remove('is-animating');
          void progress.offsetWidth;
          progress.classList.add('is-animating');
        }

        // Always reset autoplay interval to sync with timer animation
        if (autoplayEnabled && !isPaused) {
          if (autoplayInterval) clearInterval(autoplayInterval);
          autoplayInterval = setInterval(nextSlide, autoplaySpeed);
        }
      }

      function nextSlide() {
        const next = (currentSlide + 1) % totalSlides;
        showSlide(next, 'next');
      }

      function prevSlide() {
        const prev = (currentSlide - 1 + totalSlides) % totalSlides;
        showSlide(prev, 'prev');
      }

      // Event listeners
      prevBtns.forEach(btn => btn.addEventListener('click', () => { prevSlide(); resetAutoplay(); }));
      nextBtns.forEach(btn => btn.addEventListener('click', () => { nextSlide(); resetAutoplay(); }));

      dots.forEach(dot => {
        dot.addEventListener('click', () => {
          const dotIndex = parseInt(dot.dataset.slideshowDot, 10);
          const direction = dotIndex > currentSlide ? 'next' : 'prev';
          showSlide(dotIndex, direction);
          resetAutoplay();
        });
      });

      // Touch/swipe support
      let touchStartX = 0;
      let touchEndX = 0;

      container.addEventListener('touchstart', (e) => {
        touchStartX = e.changedTouches[0].screenX;
      }, { passive: true });

      container.addEventListener('touchend', (e) => {
        touchEndX = e.changedTouches[0].screenX;
        const diff = touchStartX - touchEndX;
        if (Math.abs(diff) > 50) {
          if (diff > 0) nextSlide();
          else prevSlide();
          resetAutoplay();
        }
      }, { passive: true });

      // Autoplay
      function startAutoplay() {
        if (!autoplayEnabled || isPaused) return;
        stopAutoplay();
        autoplayInterval = setInterval(nextSlide, autoplaySpeed);
        if (progress) progress.classList.add('is-animating');
      }

      function getActiveDotProgress() {
        // Find all dots matching current slide (handles both mobile and desktop)
        const activeDots = container.querySelectorAll(`[data-slideshow-dot="${currentSlide}"]`);
        const progressElements = [];
        activeDots.forEach(dot => {
          const wrapper = dot.closest('.slideshow__dot-wrapper');
          const progress = wrapper?.querySelector('[data-dot-progress]');
          if (progress) progressElements.push(progress);
        });
        return progressElements;
      }

      function stopAutoplay() {
        if (!autoplayInterval) return; // Don't pause if not running
        clearInterval(autoplayInterval);
        autoplayInterval = null;
        if (progress) progress.classList.remove('is-animating');
        // Pause dot timers (both mobile and desktop)
        getActiveDotProgress().forEach(el => el.style.animationPlayState = 'paused');
      }

      function resetAutoplay() {
        // Clear the interval without pausing dot timer (showSlide handles dot timer)
        if (autoplayInterval) {
          clearInterval(autoplayInterval);
          autoplayInterval = null;
        }
        // Restart interval
        if (!isPaused && autoplayEnabled) {
          autoplayInterval = setInterval(nextSlide, autoplaySpeed);
        }
      }

      function resumeAutoplay() {
        isPaused = false;
        // Resume dot timers (both mobile and desktop)
        getActiveDotProgress().forEach(el => el.style.animationPlayState = 'running');
        startAutoplay();
      }

      {% if pause_on_hover %}
      container.addEventListener('mouseenter', () => { isPaused = true; stopAutoplay(); });
      container.addEventListener('mouseleave', resumeAutoplay);
      container.addEventListener('focusin', () => { isPaused = true; stopAutoplay(); });
      container.addEventListener('focusout', resumeAutoplay);
      {% endif %}

      {% if parallax %}
      // Parallax effect
      if (parallaxMedia.length > 0) {
        function updateParallax() {
          const rect = container.getBoundingClientRect();
          const viewportHeight = window.innerHeight;
          if (rect.bottom < 0 || rect.top > viewportHeight) return;

          const progress = (viewportHeight - rect.top) / (viewportHeight + rect.height);
          const translateY = (progress - 0.5) * 20;
          parallaxMedia.forEach(media => {
            gsap.set(media, { y: `${translateY}%` });
          });
        }

        if (window.pieces && window.pieces.lenis && window.pieces.lenis.lenis) {
          window.pieces.lenis.lenis.on('scroll', updateParallax);
        } else {
          window.addEventListener('scroll', () => requestAnimationFrame(updateParallax), { passive: true });
        }
      }
      {% endif %}

      // Initialize first slide with intro animation
      const firstSlide = slides[0];
      if (firstSlide) {
        // Set initial color scheme from first slide
        updateColorScheme(firstSlide);
        // Animate in immediately (content is already hidden by CSS)
        animateContent(firstSlide, true);
      }

      // Initialize first dot timer animation BEFORE starting autoplay (both mobile and desktop)
      if (autoplayEnabled) {
        const firstDots = container.querySelectorAll('[data-slideshow-dot="0"]');
        firstDots.forEach(dot => {
          const wrapper = dot.closest('.slideshow__dot-wrapper');
          if (wrapper) {
            const dotTimer = wrapper.querySelector('.slideshow__dot-timer');
            const dotProgress = wrapper.querySelector('[data-dot-progress]');
            if (dotTimer) dotTimer.style.opacity = '1';
            if (dotProgress) dotProgress.classList.add('is-animating');
          }
        });
      }

      startAutoplay();
      if (progress && autoplayEnabled) progress.classList.add('is-animating');

      // Cleanup on Swup navigation
      window.addEventListener('swup:willReplaceContent', function cleanup() {
        stopAutoplay();
        window.removeEventListener('swup:willReplaceContent', cleanup);
      }, { once: true });
    }

    // Start initialization as early as possible
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initSlideshow);
    } else {
      initSlideshow();
    }
  })();
</script>

{% schema %}
{
  "name": "t:sections.slideshow.name",
  "tag": "section",
  "class": "slideshow-section-wrapper no-header-offset",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:shared.settings.color_scheme.label",
      "default": "scheme-1"
    },
    {
      "type": "range",
      "id": "section_height",
      "min": 50,
      "max": 100,
      "step": 5,
      "default": 80,
      "unit": "vh",
      "label": "t:shared.settings.section_height.label"
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "t:shared.settings.overlay_color.label",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "min": 0,
      "max": 80,
      "step": 5,
      "default": 30,
      "unit": "%",
      "label": "t:shared.settings.overlay_opacity.label"
    },
    {
      "type": "header",
      "content": "t:sections.slideshow.settings.header__transition.content"
    },
    {
      "type": "select",
      "id": "transition_effect",
      "label": "t:sections.slideshow.settings.transition_effect.label",
      "default": "fade",
      "options": [
        {
          "value": "fade",
          "label": "t:shared.options.fade"
        },
        {
          "value": "zoom-in",
          "label": "t:sections.slideshow.settings.transition_effect.options__1.label"
        },
        {
          "value": "zoom-out",
          "label": "t:sections.slideshow.settings.transition_effect.options__2.label"
        },
        {
          "value": "slide",
          "label": "t:shared.options.slide"
        },
        {
          "value": "swipe",
          "label": "t:sections.slideshow.settings.transition_effect.options__4.label"
        }
      ]
    },
    {
      "type": "header",
      "content": "t:shared.headers.autoplay"
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "default": true,
      "label": "t:shared.settings.autoplay.label"
    },
    {
      "type": "checkbox",
      "id": "pause_on_hover",
      "default": true,
      "label": "t:shared.settings.pause_on_hover.label"
    },
    {
      "type": "range",
      "id": "autoplay_speed",
      "min": 3,
      "max": 10,
      "step": 1,
      "default": 5,
      "unit": "s",
      "label": "t:shared.settings.autoplay_speed.label"
    },
    {
      "type": "select",
      "id": "progress_type",
      "label": "t:sections.slideshow.settings.progress_type.label",
      "default": "dots",
      "options": [
        {
          "value": "none",
          "label": "t:shared.options.none"
        },
        {
          "value": "dots",
          "label": "t:sections.slideshow.settings.progress_type.options__1.label"
        },
        {
          "value": "bar",
          "label": "t:shared.options.bar"
        }
      ],
      "info": "t:sections.slideshow.settings.progress_type.info"
    },
    {
      "type": "header",
      "content": "t:sections.slideshow.settings.header__navigation.content"
    },
    {
      "type": "checkbox",
      "id": "show_arrows",
      "default": true,
      "label": "t:shared.settings.show_arrows.label"
    },
    {
      "type": "checkbox",
      "id": "show_dots",
      "default": true,
      "label": "t:sections.slideshow.settings.show_dots.label"
    },
    {
      "type": "header",
      "content": "t:sections.slideshow.settings.header__effects.content"
    },
    {
      "type": "checkbox",
      "id": "parallax",
      "default": false,
      "label": "t:sections.slideshow.settings.parallax.label"
    }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "t:shared.blocks.slide",
      "settings": [
        {
          "type": "header",
          "content": "t:shared.headers.media"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "t:shared.settings.image.label"
        },
        {
          "type": "video",
          "id": "video",
          "label": "t:shared.settings.video.label",
          "info": "t:shared.info.upload_video"
        },
        {
          "type": "header",
          "content": "t:shared.headers.content"
        },
        {
          "type": "text",
          "id": "label",
          "label": "t:shared.settings.label.label"
        },
        {
          "type": "text",
          "id": "title",
          "label": "t:shared.settings.title.label",
          "default": "t:sections.slideshow.blocks.slide.settings.title.default"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "t:shared.settings.text.label"
        },
        {
          "type": "text",
          "id": "button_text",
          "label": "t:shared.settings.button_text.label"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "t:shared.settings.button_link.label"
        },
        {
          "type": "select",
          "id": "button_style",
          "label": "t:shared.settings.button_style.label",
          "default": "solid",
          "options": [
            {
              "value": "solid",
              "label": "t:shared.settings.button_style.options__0.label"
            },
            {
              "value": "outline",
              "label": "t:shared.settings.button_style.options__1.label"
            }
          ]
        },
        {
          "type": "select",
          "id": "button_color",
          "label": "t:shared.settings.button_color.label",
          "default": "primary",
          "options": [
            {
              "value": "primary",
              "label": "t:shared.settings.button_color.options__0.label"
            },
            {
              "value": "secondary",
              "label": "t:shared.settings.button_color.options__1.label"
            }
          ]
        },
        {
          "type": "select",
          "id": "button_size",
          "label": "t:shared.settings.button_size.label",
          "default": "large",
          "options": [
            {
              "value": "small",
              "label": "t:shared.settings.button_size.options__0.label"
            },
            {
              "value": "default",
              "label": "t:shared.settings.button_size.options__1.label"
            },
            {
              "value": "large",
              "label": "t:shared.settings.button_size.options__2.label"
            }
          ]
        },
        {
          "type": "header",
          "content": "t:shared.headers.layout"
        },
        {
          "type": "select",
          "id": "text_position",
          "label": "t:sections.slideshow.blocks.slide.settings.text_position.label",
          "default": "middle_center",
          "options": [
            {
              "value": "top_left",
              "label": "t:sections.slideshow.blocks.slide.settings.text_position.options__0.label"
            },
            {
              "value": "top_center",
              "label": "t:sections.slideshow.blocks.slide.settings.text_position.options__1.label"
            },
            {
              "value": "top_right",
              "label": "t:sections.slideshow.blocks.slide.settings.text_position.options__2.label"
            },
            {
              "value": "middle_left",
              "label": "t:sections.slideshow.blocks.slide.settings.text_position.options__3.label"
            },
            {
              "value": "middle_center",
              "label": "t:sections.slideshow.blocks.slide.settings.text_position.options__4.label"
            },
            {
              "value": "middle_right",
              "label": "t:sections.slideshow.blocks.slide.settings.text_position.options__5.label"
            },
            {
              "value": "bottom_left",
              "label": "t:sections.slideshow.blocks.slide.settings.text_position.options__6.label"
            },
            {
              "value": "bottom_center",
              "label": "t:sections.slideshow.blocks.slide.settings.text_position.options__7.label"
            },
            {
              "value": "bottom_right",
              "label": "t:sections.slideshow.blocks.slide.settings.text_position.options__8.label"
            }
          ]
        },
        {
          "type": "text_alignment",
          "id": "text_alignment",
          "label": "t:shared.settings.text_alignment.label",
          "default": "center"
        },
        {
          "type": "checkbox",
          "id": "full_width_content",
          "label": "t:sections.slideshow.blocks.slide.settings.full_width_content.label",
          "default": false,
          "info": "t:sections.slideshow.blocks.slide.settings.full_width_content.info"
        },
        {
          "type": "color_scheme",
          "id": "color_scheme",
          "label": "t:shared.settings.color_scheme.label",
          "default": "scheme-3"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.slideshow.presets.slideshow.name",
      "category": "t:sections.slideshow.presets.slideshow.category",
      "blocks": [
        {
          "type": "slide",
          "settings": {
            "title": "t:sections.slideshow.presets.slideshow.blocks.slide.0.title",
            "text": "t:sections.slideshow.presets.slideshow.blocks.slide.0.text"
          }
        },
        {
          "type": "slide",
          "settings": {
            "title": "t:sections.slideshow.presets.slideshow.blocks.slide.1.title",
            "text": "t:sections.slideshow.presets.slideshow.blocks.slide.1.text"
          }
        }
      ]
    }
  ],
  "disabled_on": {
    "groups": [
      "header",
      "footer"
    ]
  }
}
{% endschema %}
