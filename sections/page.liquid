{%- comment -%}
  Page Section - Skeleton + Tailwind
{%- endcomment -%}

<section class="bg-[--color-background]">
  <div class="page-container section-spacing max-w-3xl">
    <h1 class="text-3xl font-bold tracking-tight text-[--color-text] sm:text-4xl">{{ page.title }}</h1>

    <div class="mt-8 prose max-w-none text-[--color-text]">
      {{ page.content }}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Page",
  "settings": []
}
{% endschema %}
